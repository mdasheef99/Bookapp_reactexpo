# WU0B Command, Query, DTO, Error, and Rate Catalogue

**Status:** `independently_approved`
**Authority:** design only; WU0A contracts/registers remain normative

> **Unit 7A override (2026-08-12):** C08-C10's duplicate advice, action,
> target-row, increment, and create-separate semantics are **SUPERSEDED FOR UNIT
> 7A**. They are historical/deferred and are not implementation authority. Unit
> 7A uses the create-only command defined in
> [the Unit 7A SDD](../07a-create-only-inventory-commit-sdd.md).

## 1. Shared envelopes and notation

Every command request is a strict, unknown-key-rejecting object containing `contract_version`, UUID `command_id`, 16–128 character `idempotency_key`, target ID, expected target version when stateful, and the named payload. JWT is the trust boundary for user commands; service-role JWT plus a claimed task is the boundary for workers. The actor resolver derives entity ownership and final `store_id`; supplied store identity never authorizes. Every mutation records the canonical result against `(actor-or-service, operation, idempotency_key)` and rejects key reuse with a different request fingerprint.

In the tables: `Tx` means one database transaction after authorization and validation; `RO` means read-only; `V` means an expected-version predicate; `I` means canonical idempotent replay. Events contain only entity/store/actor IDs subject to access policy, operation, version, state/outcome, and bounded code/count. `T0` is IDs/outcome/duration only; `T1` adds bounded counts/categories; `T2` adds byte/dimension/MIME validation summaries. All forbid raw media/model/provider data, prompts, tokens, paths, credentials, customer PII, private notes, and authorization headers. Red references are expanded in [06](./06-red-tests-acceptance-and-handoff.md).

## 2. Command catalogue

| ID / request DTO | Actor, boundary, server authority and preconditions | Transaction, version, idempotency and surviving effect | Errors / HTTP; event; telemetry; rate; red |
| --- | --- | --- | --- |
| C01 `StartSessionRequest` | Verified initiating Owner via Owner Edge boundary; derive eligible active store; language/defaults/policy valid; no conflicting active-session policy | Tx create session/defaults; V policy snapshot; I actor+store+policy; denial leaves none | `AUTHZ`403, `CONFLICT`409, `QUOTA`429; `session.started`; T0; R2; TENANT/STATE |
| C02 `AuthorizeScanUploadRequest` | Initiating Owner; derive active session/store; purpose fixed `scan_input`; envelope within media limits | Tx persist single-use short-lived capability record; V session; I actor+session+media ordinal; failure leaves no authority | `AUTHZ`403, `STATE`409, `MEDIA`422, `QUOTA`429; `input.upload_authorized`; T2; R3; UPLOAD/TENANT |
| C03 `SubmitAcceptedInputRequest` | Initiating Owner; verify capability, sanitized media identity/hash, purpose and session | Tx link input + reserve orchestration/cost identity; V session/capability; I media hash+session; replay returns job identity | `MEDIA`422, `STATE`409, `REPLAY_MISMATCH`409, `QUOTA`429; `input.accepted`; T1; R3; UPLOAD/WORKER |
| C04 `CloseSessionRequest` | Initiating Owner; derive session/store; every accepted input terminal | Tx `active→closing→closed` summary; V session; I actor+session+close; nonterminal failure leaves active | `AUTHZ`403, `STATE`409, `VERSION`409; `session.close_requested`, `session.closed`; T1; R2; STATE |
| C05 `UpdateCandidateReviewRequest` | Initiating Owner; candidate belongs to active/closed reviewable session; strict WU0A metadata/condition fields | Tx staged snapshot only; V candidate; I actor+candidate+action; failure leaves prior review | `AUTHZ`403, `VERSION`409, `VALIDATION`422; `candidate.review_updated`; T1; R2; TENANT/STATE/DTO |
| C06 `AddMissedCandidateRequest` | Initiating Owner; derive session/input; bounded manual identity; session reviewable and under candidate cap | Tx create staged manual candidate; V session/input; I session+manual ordinal; no provider/job effect | `STATE`409, `LIMIT`422, `VALIDATION`422; `candidate.manual_added`; T1; R2; STATE/DTO |
| C07 `SkipCandidateRequest` | Initiating Owner; candidate reviewable; bounded reason code | Tx records review disposition `skipped_false_detection`, explicitly not a candidate state; persisted candidate state remains the current Master §6 value and the candidate is excluded from commit; V candidate; I candidate+skip; replay returns the recorded disposition | `AUTHZ`403, `STATE`409, `VERSION`409; `candidate.skipped`; T1; R2; STATE |
| C08 `CommitCandidateRequest(action=create_private)` | Initiating Owner; complete review; recompute same-store duplicate advice; valid quantity/media/condition | Tx private inventory + candidate link + audit only; V candidate/duplicate snapshot; I candidate+commit; publication excluded | WU0A `P9_*` including `P9_DUPLICATE_TARGET_CHANGED`409 and `P9_QUANTITY_INVARIANT_FAILED`409; `candidate.committed_private`; T1; R1; COMMIT/TENANT |
| C09 `CommitCandidateRequest(action=increment_match)` | Initiating Owner; target same store and compatible under locked recomputation | Tx lock target; increment total+available only; V candidate+inventory; I candidate+commit; failure leaves both unchanged | `P9_DUPLICATE_TARGET_CHANGED`409, `P9_QUANTITY_INVARIANT_FAILED`409; `inventory.quantity_incremented`; T1; R1; COMMIT |
| C10 `CommitCandidateRequest(action=create_separate)` | Initiating Owner; explicit separate decision after duplicate warning; complete review | Tx new private inventory and candidate link; V candidate/advice; I candidate+commit; request photos ignored | `VERSION`409, `VALIDATION`422, `P9_QUANTITY_INVARIANT_FAILED`409; `inventory.created_separate`; T1; R1; COMMIT |
| C11 `RequestPublicationRequest` | Same-store authorized Owner; derive inventory/store; eligible approved public metadata/media | Tx record intent then separate projection Tx; V inventory; I inventory+publication version; inventory survives projection failure | `P9_MEDIA_NOT_APPROVED`422, `P9_PUBLICATION_FAILED`202; `publication.requested|published|failed`; T1; R2; PUBLICATION/DTO |
| C12 `RetryPublicationRequest` | Shared authenticated actor dispatcher selects exactly one path: same-store Owner JWT or service JWT with a claimed publication-retry job; both reauthorize inventory and eligibility | Projection-only Tx; V intent/inventory; I original publication identity; never changes inventory quantity/existence | `AUTHZ`403, `STATE`409, `P9_PUBLICATION_FAILED`202; `publication.retried`; T1; Owner R2 / worker R4; PUBLICATION |
| C13 `MarkNeedsReviewRequest` | Initiating Owner; derive candidate/session; bounded reason | Tx mark review state; V candidate/session; I candidate+reason action; no inventory effect | `AUTHZ`403, `STATE`409, `VERSION`409; `candidate.needs_review`; T1; R2; STATE |
| C14 `RequestCurrentCopyPhotosRequest` | Owning customer through customer boundary; derive request item/store/customer; count 1–3 and item eligible | Tx create/version photo substate; V request item; I customer+item+request; no hold/payment effect alone | `AUTHZ`403, `STATE`409, `LIMIT`422; `photos.requested`; T1; R2; PHOTO/TENANT |
| C15 `AuthorizeRequestPhotoUploadRequest` | Authorized owning-store Owner; derive request item/store; state `requested|uploading`; sequence 1–3 | Tx single-use `customer_request` capability; V photo request; I request+sequence; failure leaves no media link | `AUTHZ`403, `STATE`409, `MEDIA`422, `LIMIT`422; `photos.upload_authorized`; T2; R3; PHOTO/UPLOAD |
| C16 `SupplyRequestPhotosRequest` | Owning-store Owner; 1–3 newly captured request-purpose media IDs in `uploading`; derive request item/store | Tx record pending links and validation job identity; V photo request; I request+media-set hash; only private staged links survive | `MEDIA`422, `STATE`409, `VERSION`409; `photos.supplied`; T1; R2; PHO-MEDIA/LIFECYCLE |
| C17 `AcceptRequestPhotosRequest` | Owning customer; require `provided`, current Owner confirmation snapshot, request `awaiting_customer_decision`, and active unexpired soft hold | Existing Phase 6 customer-accept Tx promotes soft→firm hold and may enter `payment_ready`; V request+confirmation; I customer+request+accept; canonical Phase 6 effect survives | `AUTHZ`403, `STATE`409, `VERSION`409, `HOLD`409; `photos.accepted`; T1; R1; PHO-ACCEPT/PHASE6 |
| C18 `DeclineRequestPhotosRequest` | Owning customer; require current confirmed photo proposal; derive request/items/customer | Existing Phase 6 decline/withdraw Tx releases soft hold and recalculates; V request+confirmation; I customer+request+decline; no paid-order effect | `AUTHZ`403, `STATE`409, `VERSION`409; `photos.declined`; T1; R1; PHO-DECLINE/PHASE6 |
| C19 `MarkRequestItemUnfulfilledRequest` | Authorized owning-store Owner; derive item/store; bounded reason; any nonterminal photo proposal | Tx mark unavailable/unfulfilled and invoke Phase 6 release/recalculation; V request item; I item+unfulfilled; any active soft hold is released | `AUTHZ`403, `STATE`409, `VERSION`409; `photos.unfulfilled`; T1; R1; PHO-UNFULFILLED/PHASE6 |
| C20 `AuthorizePublicCopyUploadRequest` | Same-store Owner; derive candidate/inventory/store; purpose fixed `public_copy`; approved role/envelope | Tx single-use public-copy capability; V entity; I entity+role+ordinal; no public link/publication effect | `AUTHZ`403, `STATE`409, `MEDIA`422, `LIMIT`422; `public_media.upload_authorized`; T2; R3; UPLOAD/DTO |
| C21 `SubmitPublicCopyMediaRequest` | Same-store Owner; approved sanitized public-copy IDs only; role/order bounds | Tx link approval evidence and recompute eligibility; V entity; I entity+media-set hash; request/scan media rejected | `MEDIA`422, `STATE`409, `VERSION`409; `public_media.submitted`; T1; R2; UPLOAD/PUBLICATION |
| C22 `InventoryEditRequest(action=metadata)` | Same-store Owner; derive inventory/store; WU0A-bounded store snapshot; canonical truth immutable | Tx store fields + rematch/eligibility intent; V inventory; I inventory+edit command; previous snapshot survives denial | `AUTHZ`403, `VERSION`409, `VALIDATION`422; `inventory.metadata_updated`; T1; R2; EDIT/DTO |
| C23 `InventoryEditRequest(action=quantity)` | Same-store Owner; derive inventory/store; bounded transfer and active-hold compatibility | Tx lock inventory/holds; preserve bucket equality; V inventory; I inventory+adjustment; failure leaves quantities unchanged | `P9_QUANTITY_INVARIANT_FAILED`409, `VERSION`409; `inventory.quantity_adjusted`; T1; R1; COMMIT/EDIT |
| C24 `InventoryEditRequest(action=commercial_details)` | Same-store Owner; bounded price, shelf/location, public/internal notes; public/private classes enforced | Tx store fields + safe projection refresh if public fields changed; V inventory; I command; prior projection survives failure truthfully | `AUTHZ`403, `VERSION`409, `VALIDATION`422; `inventory.commercial_details_updated`; T1; R2; EDIT/DTO |
| C25 `InventoryEditRequest(action=condition_damage_media)` | Same-store Owner; condition/damage/sellability and approved public media only | Tx disclosure/link update; atomically retract/block ineligible projection; V inventory; I command; private inventory survives | `MEDIA`422, `VALIDATION`422, `VERSION`409; `inventory.condition_damage_updated`; T1; R2; EDIT/PUBLICATION |
| C26 `InventoryEditRequest(action=publication_state)` | Same-store Owner; intent `private|publish|pause`; eligibility reauthorized | Tx intent; C11/C12 projection semantics; V inventory; I command; private/pause retracts projection only | `P9_MEDIA_NOT_APPROVED`422, `P9_PUBLICATION_FAILED`202, `VERSION`409; `publication.intent_changed|retracted`; T1; R2; PUBLICATION |
| C27 `CompleteRequestPhotoValidationJob` | Worker-only claimed media-validation job; derive media/request/store/purpose from job | Tx record sanitized validation and, when every supplied link passes, `uploading→provided`; V media/photo request+lease; I job identity; invalid media leaves request nonprovided | `AUTHZ`403, `MEDIA`422, `STATE`409; `photos.media_validated|validation_failed`; T2; R4; PHO-MEDIA/WORKER |
| C28 `ConfirmRequestPhotoItemRequest` | Owning-store Owner; require `provided`; confirm still available, current sellable quantity, price and terms | One Phase 6 Owner-confirmation Tx calls C30, snapshots price/quantity/terms, sets `awaiting_customer_decision`; V request/item/inventory; I request+proposal; prior proposal/hold replaced atomically | `AUTHZ`403, `STATE`409, `VERSION`409, `QUANTITY`409; `photos.owner_confirmed`; T1; R1; PHO-CONFIRM/PHASE6 |
| C29 `ExpireRequestPhotoSoftHoldJob` | Worker/system with claimed Phase 6 expiry task; require expired active soft hold and unchanged request version | Existing Phase 6 expiry Tx releases hold, expires proposal/photo evidence as policy requires, recalculates request; V task/request/hold; I task identity | `AUTHZ`403, `STATE`409, `VERSION`409; `photos.hold_expired`; T1; R4; PHO-HOLD/WORKER |
| C30 `CreateOrRefreshRequestSoftHold` | Internal transaction-owning Postgres operation callable only by C28; never client executable | Lock request/item/inventory/holds; validate current available quantity and price bound; atomically release superseded soft hold and create bounded soft hold from Phase 6 policy; V all locked versions; I Owner confirmation command | `AUTHZ`403, `STATE`409, `VERSION`409, `QUANTITY`409, `POLICY`500; `photos.soft_hold_created|refreshed`; T1; R4; PHO-HOLD/PHASE6 |

## 3. Query catalogue

All queries are RO, have no mutation idempotency key, reject unknown parameters, bind authorization before selection, return safe projection DTOs only, and emit query kind/outcome/duration/counts without query text or returned content.

| ID / request → response | Actor, boundary, authority and preconditions | Ordering/version/cache; failure effects | Errors / HTTP; rate; red |
| --- | --- | --- | --- |
| Q01 `SessionSummaryQuery` → `OwnerSessionSummary` | Initiating Owner; Owner boundary derives session/store | session ID; contract version; private/no shared cache; RO none | `AUTHZ`403, `NOT_FOUND`404; R2; TENANT/DTO |
| Q02 `SessionCandidatesQuery` → `OwnerCandidatePage` | Initiating Owner; derive session/store | ordinal then ID cursor; private/no shared cache | `AUTHZ`403, `CURSOR`400; R2; TENANT/DTO |
| Q03 `NeedsReviewQuery` → `OwnerNeedsReviewPage` | Same-store authorized Owner; derive store | updated-at then ID cursor; short private cache with store/version key | `AUTHZ`403, `CURSOR`400; R2; TENANT/DTO |
| Q04 `CandidateDetailQuery` → `OwnerCandidateDetail` | Initiator while session-bound; later same-store access only via approved policy | candidate/version; no shared cache | `AUTHZ`403, `NOT_FOUND`404; R2; TENANT/DTO |
| Q05 `OwnerInventoryQuery` → `OwnerInventoryDetail` | Same-store Owner; derive inventory/store | inventory version; private short cache | `AUTHZ`403, `NOT_FOUND`404; R2; TENANT/DTO |
| Q06 `PublicationStatusQuery` → `OwnerPublicationStatus` | Same-store Owner; derive inventory/store | intent/projection version; private short cache | `AUTHZ`403, `NOT_FOUND`404; R2; PUBLICATION/DTO |
| Q07 `InternalBookMatchQuery` → `InternalMatchSet` | Service-only inside Q08; no client callable boundary | query/ranking version; no independent client cursor/cache | `AUTHZ`403, `VALIDATION`422; R4; MARKET/DTO |
| Q08 `MarketplaceStoreSearchQuery` → `PublicStoreGroupPage` | Public boundary; eligible public projection only | context-authenticated cursor, rank then `store_id`; public versioned cache | `CURSOR`400, `RATE`429; R5; MARKET/DTO |
| Q09 `StorefrontCatalogueQuery` → `PublicStorefrontCatalogue` | Public; active public store/profile/projection only | complete stable title/offer ordering; optional pinned match context | `NOT_FOUND`404, `CURSOR`400; R5; MARKET/DTO |
| Q10 `ListingDetailQuery` → `PublicListingDetail` | Public; eligible projection only | listing/projection version; public cache | `NOT_FOUND`404; R5; MARKET/DTO |
| Q11 `RequestPhotoStatusQuery` → actor-specific `CustomerPhotoStatus` or `OwnerPhotoStatus` | Shared authenticated actor dispatcher selects exactly one path: owning customer JWT derives request/customer or owning-store Owner JWT derives request/store administration; projections never union | request version; private/no shared cache; capability fetched separately after reauth | `AUTHZ`403, `NOT_FOUND`404; R2; PHOTO/TENANT/DTO |

## 4. Per-operation boundary and traceability

Boundary codes select exactly one primary boundary: `OE` Owner-authenticated Edge Function; `CE` customer-authenticated Edge Function; `AE` authenticated actor-dispatch Edge Function that rejects mixed/unknown credentials and selects one closed caller-specific authorization/projection path before shared logic; `PE` public/authenticated Edge Function; `IE` internal Edge Function/service operation; `RPC` transaction-owning Postgres function; `WH` worker-only job handler; `MQ` service-internal matching/query stage. C12 `AE` is owned only by `supabase/functions/image-inventory-publication-retry/index.ts`; Q11 `AE` is owned by the request-photo boundary. External boundaries validate/authenticate/orchestrate; the named transaction owner alone locks/writes. Grant `X` means authenticated EXECUTE on the boundary but no direct table access; `S` means service-only; `P` means safe public projection. Every row derives `store_id` from target ownership/membership (`D`) or public eligibility (`PUB`).

WU0A references: `V` validation matrix/limits; `E` error catalogue; `G` grant controls; `M` marketplace query register; `R` red gates; `P` deterministic session/quantity/publication policies; `A` adapter contracts. `I` means the command boundary owns canonical request fingerprint/replay while the transaction RPC persists it; queries are RO/no mutation idempotency. The response names are positive safe projections from §5.

| ID / caller / exposure | Primary boundary and selection reason | Auth; tenancy; RLS/grant | Transaction owner; idempotency | Request → response | Governing SDD/acceptance; WU0A; red; future unit |
| --- | --- | --- | --- | --- | --- |
| C01 Owner / exposed | OE; user workflow/orchestration | JWT Owner; D; X+RLS | session-create RPC; I | `StartSessionRequest`→`OwnerSessionSummary` | MAS-12, EXT-06; V/E/G/P/R; RT-INIT-01; U2 |
| C02 Owner / exposed | OE; capability issuance after auth | JWT initiator; D; X+RLS | upload-capability RPC; I | `AuthorizeScanUploadRequest`→`UploadCapability` | MED-01, MED-13, EXT-01; V/E/G/R; RT-MEDIA-01; U3 |
| C03 Owner / exposed | OE; validates capability then orchestrates job | JWT initiator; D; X+RLS | accept-input RPC; I | `SubmitAcceptedInputRequest`→`OwnerInputReceipt` | MAS-01/02, EXT-01/02; V/E/G/P/R; RT-STATE-01; U2 |
| C04 Owner / exposed | OE; close authorization/summary | JWT initiator; D; X+RLS | close-session RPC; I | `CloseSessionRequest`→`OwnerSessionSummary` | MAS-12, EXT-06, REV-14; E/G/P/R; RT-CLOSE-01; U2 |
| C05 Owner / exposed | OE; strict review DTO | JWT initiator; D; X+RLS | review-candidate RPC; I | `UpdateCandidateReviewRequest`→`OwnerCandidateDetail` | MAS-05, REV-01/02; V/E/G/R; RT-REVIEW-01; U7 |
| C06 Owner / exposed | OE; manual candidate orchestration | JWT initiator; D; X+RLS | manual-candidate RPC; I | `AddMissedCandidateRequest`→`OwnerCandidateDetail` | REV-12, EXT-16; V/E/G/R; RT-STATE-02; U7 |
| C07 Owner / exposed | OE; explicit false detection | JWT initiator; D; X+RLS | skip-candidate RPC; I | `SkipCandidateRequest`→`OwnerCandidateDetail` | REV-12/14; E/G/P/R; RT-STATE-03; U7 |
| C08 Owner / exposed | OE; create choice and truthful result | JWT initiator; D; X+RLS | commit-candidate RPC; I | `CommitCandidateRequest(create_private)`→`CommitResult` | MAS-05/06, REV-01/05/13; V/E/G/P/R; RT-COMMIT-01; U7 |
| C09 Owner / exposed | OE; increment choice | JWT initiator; D; X+RLS | commit-candidate RPC; I | `CommitCandidateRequest(increment_match)`→`CommitResult` | DAT-16/18, REV-10/13; E/G/P/R; RT-QTY-01; U7 |
| C10 Owner / exposed | OE; separate-copy choice | JWT initiator; D; X+RLS | commit-candidate RPC; I | `CommitCandidateRequest(create_separate)`→`CommitResult` | DAT-16/19, REV-07/10; E/G/P/R; RT-COMMIT-02; U7 |
| C11 Owner / exposed | OE; publication authorization | JWT same-store; D; X+RLS | publication-intent/projection RPCs; I | `RequestPublicationRequest`→`OwnerPublicationStatus` | MAS-07/11, REV-18; E/G/P/R; RT-PUB-01; U7 |
| C12 Owner/worker / shared endpoint | AE; exact owner `supabase/functions/image-inventory-publication-retry/index.ts`; Owner/worker entrypoints do not implement C12 | Owner path: JWT same-store, D, X+RLS; worker path: service JWT+claimed publication job, D, S+RLS; mixed/unknown denied | one shared publication-retry service→projection RPC; caller-kind+identity scoped I | `RetryPublicationRequest`→`OwnerPublicationStatus|InternalJobResult` | MAS-11, REV-18; E/G/P/R; RT-PUB-02; U7 |
| C13 Owner / exposed | OE; bounded review reason | JWT initiator; D; X+RLS | candidate-state RPC; I | `MarkNeedsReviewRequest`→`OwnerCandidateDetail` | REV-14, EXT-12; E/G/P/R; RT-STATE-04; U7 |
| C14 customer / exposed | CE; customer-owned request action | JWT customer; D; X+RLS | photo-request RPC; I | `RequestCurrentCopyPhotosRequest`→`CustomerPhotoStatus` | PHO-01, PHO-08; V/E/G/R; RT-PHOTO-01; U9 |
| C15 Owner / exposed | OE; request-purpose capability | JWT owning-store; D; X+RLS | request-upload-capability RPC; I | `AuthorizeRequestPhotoUploadRequest`→`UploadCapability` | PHO-02, MED-12/13; V/E/G/R; RT-MEDIA-02; U9 |
| C16 Owner / exposed | OE; supply and enqueue validation | JWT owning-store; D; X+RLS | supply-photo RPC; I | `SupplyRequestPhotosRequest`→`OwnerPhotoStatus` | PHO-02/03, MED-12; V/E/G/R; RT-PHOTO-02; U9 |
| C17 customer / exposed | CE; acceptance guarded by proposal/hold | JWT customer; D; X+RLS | existing Phase 6 customer-accept RPC; I | `AcceptRequestPhotosRequest`→`CustomerRequestProjection` | PHO-04/05/08, DOC-6 §§4.1/6; E/G/R; RT-PHOTO-ACCEPT-01, RT-PHOTO-ACCEPT-02; U9 |
| C18 customer / exposed | CE; decline/recalculation | JWT customer; D; X+RLS | existing Phase 6 decline/cancel RPC; I | `DeclineRequestPhotosRequest`→`CustomerRequestProjection` | PHO-07/08, DOC-6 §4.1; E/G/R; RT-PHOTO-DECLINE-01; U9 |
| C19 Owner / exposed | OE; unavailable outcome | JWT owning-store; D; X+RLS | Phase 6 unavailable/recalc RPC; I | `MarkRequestItemUnfulfilledRequest`→`OwnerRequestProjection` | PHO-06/07/08; E/G/R; RT-PHOTO-UNFULFILLED-01; U9 |
| C20 Owner / exposed | OE; public-copy capability | JWT same-store; D; X+RLS | public-upload-capability RPC; I | `AuthorizePublicCopyUploadRequest`→`UploadCapability` | MED-11/13/15; V/E/G/R; RT-MEDIA-03; U3 |
| C21 Owner / exposed | OE; approved derivative linking | JWT same-store; D; X+RLS | public-media-link RPC; I | `SubmitPublicCopyMediaRequest`→`OwnerMediaStatus` | MED-11/15, REV-09; V/E/G/R; RT-MEDIA-04; U3 |
| C22 Owner / exposed | OE; metadata edit/rematch orchestration | JWT same-store; D; X+RLS | inventory-edit RPC; I | `InventoryEditRequest(metadata)`→`OwnerInventoryDetail` | DAT-08, REV-15/16/17; V/E/G/R; RT-EDIT-01; U7 |
| C23 Owner / exposed | OE; quantity transfer choice | JWT same-store; D; X+RLS | inventory-quantity RPC; I | `InventoryEditRequest(quantity)`→`OwnerInventoryDetail` | DAT-18, REV-13/15; E/G/P/R; RT-QTY-02; U7 |
| C24 Owner / exposed | OE; classified commercial edit | JWT same-store; D; X+RLS | inventory-edit RPC; I | `InventoryEditRequest(commercial_details)`→`OwnerInventoryDetail` | REV-02/15, MKT-09/12; V/E/G/R; RT-EDIT-02; U7 |
| C25 Owner / exposed | OE; condition/media eligibility | JWT same-store; D; X+RLS | inventory-edit+projection RPC; I | `InventoryEditRequest(condition_damage_media)`→`OwnerInventoryDetail` | DAT-21/24, REV-09/11/15; V/E/G/R; RT-EDIT-03; U7 |
| C26 Owner / exposed | OE; publication intent | JWT same-store; D; X+RLS | publication-intent/projection RPC; I | `InventoryEditRequest(publication_state)`→`OwnerPublicationStatus` | MAS-07/11, REV-15/18; E/G/P/R; RT-PUB-03; U7 |
| C27 worker / internal | WH; media processor result requires claim | service JWT+lease; D; S+RLS | media-result RPC; job I | `MediaValidationJob`→`InternalJobResult` | PHO-02/03, MED-14/19; V/E/G/R; RT-MEDIA-05; U9 |
| C28 Owner / exposed | OE; current qty/price confirmation | JWT owning-store; D; X+RLS | Phase 6 Owner-confirmation RPC calling C30; I | `ConfirmRequestPhotoItemRequest`→`OwnerRequestProjection` | PHO-04/08, DOC-6 §§4.1/6; V/E/G/R; RT-PHOTO-CONFIRM-01, RT-PHOTO-CONFIRM-02; U9 |
| C29 worker / internal | WH; expiry must be task/lease scoped | service JWT+lease; D; S+RLS | existing Phase 6 expiry RPC; job I | `RequestPhotoHoldExpiryJob`→`InternalJobResult` | PHO-07/08/11, DOC-6 §6; E/G/R; RT-PHOTO-HOLD-02; U9 |
| C30 C28 only / internal | RPC; locking/soft-hold atomicity | C28 definer context only; D; S, no client EXECUTE | self; confirmation-command I | `SoftHoldMutation`→`SoftHoldResult` | PHO-04/08, DOC-6 §6.2; E/G/P/R; RT-PHOTO-HOLD-01, RT-PHOTO-HOLD-03; U9 |
| Q01 Owner / exposed | OE; private session projection | JWT initiator; D; X+RLS | RO repository; none | `SessionSummaryQuery`→`OwnerSessionSummary` | EXT-06, MAS-12; V/E/G/R; RT-INIT-01; U2 |
| Q02 Owner / exposed | OE; private candidate page | JWT initiator; D; X+RLS | RO repository; none | `SessionCandidatesQuery`→`OwnerCandidatePage` | REV-14, MAS-12; V/E/G/R; RT-TENANT-01; U2 |
| Q03 Owner / exposed | OE; same-store review queue | JWT same-store; D; X+RLS | RO repository; none | `NeedsReviewQuery`→`OwnerNeedsReviewPage` | REV-04/14; V/E/G/R; RT-TENANT-01; U7 |
| Q04 Owner / exposed | OE; actor-safe detail | JWT initiator/policy; D; X+RLS | RO repository; none | `CandidateDetailQuery`→`OwnerCandidateDetail` | REV-01/14; V/E/G/R; RT-TENANT-01; U7 |
| Q05 Owner / exposed | OE; private inventory detail | JWT same-store; D; X+RLS | RO repository; none | `OwnerInventoryQuery`→`OwnerInventoryDetail` | REV-15, MKT-12; V/E/G/R; RT-TENANT-01; U7 |
| Q06 Owner / exposed | OE; publication safe status | JWT same-store; D; X+RLS | RO repository; none | `PublicationStatusQuery`→`OwnerPublicationStatus` | REV-18/19; E/G/R; RT-PUB-01; U7 |
| Q07 service / internal | MQ; raw matching must not be client callable | service context; PUB; S | RO match repository; none | `InternalBookMatchQuery`→`InternalMatchSet` | MKT-02/05/14, DAT-14; V/M/G/R; RT-MARKET-01; U8 |
| Q08 public / exposed | PE; grouped-store public contract | optional JWT; PUB; P+RLS | safe query/projection function; none | `MarketplaceStoreSearchQuery`→`PublicStoreGroupPage` | MAS-AC06, MKT-01/02/11/14; V/M/G/R; RT-MARKET-02, RT-MARKET-03; U8 |
| Q09 public / exposed | PE; complete storefront projection | optional JWT; PUB; P+RLS | safe query/projection function; none | `StorefrontCatalogueQuery`→`PublicStorefrontCatalogue` | MKT-03/04/10; V/M/G/R; RT-MARKET-04; U8 |
| Q10 public / exposed | PE; eligible listing projection | optional JWT; PUB; P+RLS | safe projection function; none | `ListingDetailQuery`→`PublicListingDetail` | MKT-06/08/10/12; V/M/G/R; RT-PRIVACY-01; U8 |
| Q11 customer/Owner / request-photo endpoint | AE; request-photo boundary selects customer or Owner projection before querying | Customer path: JWT request owner, D, X+RLS; Owner path: JWT administering request store, D, X+RLS; mixed/unknown denied | separate safe customer/Owner projection RPC branches; none | `RequestPhotoStatusQuery`→exactly one of `CustomerPhotoStatus|OwnerPhotoStatus` | PHO-05/09/10, MED-03/16; V/E/G/R; RT-PHOTO-TENANT-01; U9 |

## 5. Requirement-to-operation traceability

| SDD requirement / acceptance | Commands/queries and DTOs | Boundary / transaction or projection rule | Red tests | Unit |
| --- | --- | --- | --- | --- |
| MAS-01/02, EXT-01–10 | C02,C03,C27; input/job DTOs | OE/WH; sanitized strict adapter evidence before persisted transitions | RT-MEDIA-01, RT-STATE-01, RT-MODEL-01 | U2–U4 |
| MAS-03/12, MAS-AC04/09 | C01–C30, Q01–Q06,Q11; private projections | user boundaries derive target store; RPC/RLS backstop | RT-TENANT-01, RT-FORGED-01, RT-INIT-01 | U2,U3,U7,U9 |
| MAS-05/06/11, DAT-16–19, REV-01/05/10/13/18 | C05–C12,C23,C25,C26; commit/publication DTOs | OE for Owner-only operations; AE for C12 Owner/claimed-worker dispatch; locked commit/edit/projection RPCs; replay canonical | RT-REVIEW-01, RT-COMMIT-01/02, RT-QTY-01/02, RT-PUB-01/02/03 | U7 |
| MAS-07, MAS-AC06, MKT-01–14 | Q07–Q10; internal match/public store/listing DTOs | MQ internal matching; PE grouped public projection before pagination | RT-MARKET-01–04, RT-PRIVACY-01 | U8 |
| MAS-08, MED-01–19 | C02,C15,C16,C20,C21,C27,Q11; capability/media DTOs | purpose-separated OE/CE/WH; AE selects Q11 customer/Owner projection; media RPCs remain purpose-bound | RT-MEDIA-01–05, RT-PHOTO-TENANT-01, RT-LIFECYCLE-01 | U3,U9,U10 |
| PHO-01–14, DOC-6 §§4.1/6 | C14–C19,C27–C30,Q11; confirmation/hold projections | CE/OE/WH plus Q11 AE actor dispatch; Owner confirmation+C30 soft hold before C17 acceptance | RT-PHOTO-01/02, RT-PHOTO-CONFIRM-01/02, RT-PHOTO-ACCEPT-01/02, RT-PHOTO-HOLD-01/02/03 | U9 |
| MAS-09, MAS-AC08, PHO-13 | C17–C19,C28–C30 | existing pre-payment Phase 6 seams only | RT-SCOPE-01 | U9 |
| EXT-11–18, MED-22 | provider jobs/adapters; internal DTOs | IE/WH outside DB transaction; cost reservation RPC only | RT-PROVIDER-01, RT-COST-01 | U4,U5 |
| MAS-AC07, MED-10/19 | C29 and lifecycle jobs | WH→hold-aware idempotent lifecycle RPC | RT-LIFECYCLE-01 | U10 |

## 6. DTO projection inventory

| Projection | Positive allowlist |
| --- | --- |
| Owner session/candidate | IDs, ordinal, bounded reviewed metadata/provenance summary, warnings, state/version, actionable stable errors and counts |
| Owner inventory/publication | Store-owned fields, approved private quantity buckets, condition/damage, media role IDs, intent/projection status/version |
| Customer photo | Own request/item IDs, photo state/version, approved evidence metadata; short-lived media capability only in dedicated response |
| Public store group | WU0A register fields: public store identity/locality, match summary, availability band, cover, price-from, condition/damage summary, counts and cursor |
| Public storefront/listing | Approved authoritative metadata, price, condition/damage, public-copy media and disclosures; complete active catalogue semantics |
| Internal match/job | Opaque IDs, contract/policy versions, bounded scores/reasons, attempt/lease metadata and provider reference IDs; never exposed externally |

## 7. Stable error-to-HTTP catalogue

WU0A's authoritative register now contains the original seven errors plus `P9_AUTH_REQUIRED`401, `P9_REQUEST_INVALID`400, `P9_NOT_FOUND`404, `P9_STATE_CONFLICT`409, `P9_VERSION_CONFLICT`409, `P9_IDEMPOTENCY_MISMATCH`409, `P9_LIMIT_EXCEEDED`422, `P9_QUOTA_EXCEEDED`429, `P9_RATE_LIMITED`429, `P9_CURSOR_INVALID`400, `P9_SOFT_HOLD_REQUIRED`409, `P9_INSUFFICIENT_AVAILABLE_QUANTITY`409, `P9_POLICY_CONFIGURATION_INVALID`500, and `P9_INTERNAL_ERROR`500. `PHASE9_OPERATION_ERROR_CODES` maps every C01–C30 and Q01–Q11 operation to exact registered codes; every definition includes retryability, safe message, severity, surviving effect, and idempotency-key reuse. Table shorthand is explanatory only and cannot override that register.

## 8. Rate and abuse classes

| Class | Boundary | Design |
| --- | --- | --- |
| R1 critical mutation | Commit, quantity, Owner photo confirmation/soft hold, customer decision, unfulfilled | Per actor+store/entity; low burst; idempotent retries exempt only after fingerprint match |
| R2 normal private | Review/edit/status/session/photo actions | Per actor+store plus entity caps; suspicious cross-entity failures tighten limits |
| R3 upload authority | All three capability issuers | Per actor+store+IP, daily store media quota, purpose/entity/ordinal hard caps |
| R4 service/worker | Claims, retry, internal match | Service identity+kind+store; batch/attempt/lease caps; circuit breaker |
| R5 public query | Marketplace/storefront/listing | Per IP/session, normalized-context cache, bounded page size 50; enumeration anomaly controls |

Locked hard limits are 15 candidates, one whole-image vision fallback, 1–3 request/public-copy photos, WU0A field bounds, and strict payload size. Concrete time windows, quotas, and capability TTLs are `DB_AUDIT_REQUIRED_BEFORE_DATABASE_DESIGN` or later configuration approval; they may not be unbounded.
